<UserControl x:Class="LibraryDemo.DesktopClient.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryDemo.DesktopClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="DarkSalmon">
        <ListView Margin="100" Name="lvAuthors" ItemsSource="{Binding ListOfLibraries}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="420" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Address" Width="920" DisplayMemberBinding="{Binding Address}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
