<UserControl x:Class="LibraryDemo.DesktopClient.Views.AuthorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryDemo.DesktopClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="DarkSalmon">
        <ListView Margin="100" Name="lvAuthors" ItemsSource="{Binding ListOfAuthors}"
          SelectedItem="{Binding Path=SelectedAuthor}">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Name" Width="420" DisplayMemberBinding="{Binding Name}" />
					<GridViewColumn Header="Gender" Width="200" DisplayMemberBinding="{Binding Gender}" />
					<GridViewColumn Header="Mail" Width="720">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Email}" TextDecorations="Underline" Foreground="Blue" Cursor="Hand" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView>
			</ListView.View>
		</ListView>
    </Grid>
</UserControl>
