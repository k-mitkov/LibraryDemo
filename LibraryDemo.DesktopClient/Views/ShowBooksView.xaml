<UserControl x:Class="LibraryDemo.DesktopClient.Views.ShowBooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryDemo.DesktopClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="DarkSalmon">
        <ListView Margin="100" Name="lvBooks" ItemsSource="{Binding ListOfBooks}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{Binding TitleHeader}" Width="150" DisplayMemberBinding="{Binding Title}" />
                    <GridViewColumn Header="{Binding AuthorHeader}" Width="520" DisplayMemberBinding="{Binding Author}" />
                    <GridViewColumn Header="{Binding PriceHeader}" Width="80" DisplayMemberBinding="{Binding Price}" />
                    <GridViewColumn Header="{Binding LibraryHeader}" Width="580" DisplayMemberBinding="{Binding Library}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
