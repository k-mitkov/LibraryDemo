<UserControl x:Class="LibraryDemo.DesktopClient.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryDemo.DesktopClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="DarkSalmon">
        <StackPanel Margin="100"  Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="txt" Text="{Binding SelectLanguage}" FontSize="30" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Path= ListOfLanguages}" 
                      Width="500" Height="30" HorizontalAlignment="Left"
                      SelectedItem="{Binding Path=Slanguage}" >
            </ComboBox>
            <Button Content="{Binding SaveButtonContent}" Command="{Binding Path=SaveCommand}" Background="Moccasin"
                    FontSize ="20" VerticalAlignment="Center" Width="250" Height="30" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</UserControl>
